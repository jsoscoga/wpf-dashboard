{
  "ConnectionStrings": {
    "uno": "Server=<Server\\Path>;Database=WERMAWIN;Trusted_Connection=True;User Id=<User>;Password=<Password>" // Change this parameter by production database credentials
  },
  "SecondsForInterval": 30, // Interval of 30 seg is mandatory
  "MacIds": "<MacIds>" // Change this parameter by production slave mac Id's
}